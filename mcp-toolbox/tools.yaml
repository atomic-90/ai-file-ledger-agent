sources:
  my-bq-source:
    kind: bigquery
    project: ai-file-ledger-agent

tools:
  run_query:
    kind: bigquery-sql
    source: my-bq-source
    description: Execute SQL queries on BigQuery
    statement: "{{query}}"

toolsets:
  my_bq_toolset:
    - run_query
